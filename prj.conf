# 1. Hardware UART Console (Physical pins)
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# 2. I2C Configuration for manual driver
CONFIG_I2C=y

# 3. Optional: Helpful for raw driver debugging
CONFIG_DEBUG=y